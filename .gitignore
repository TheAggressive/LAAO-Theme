/vendor
/node_modules
/dist
npm-debug.log
yarn-error.log
.vscode/
.phpunit.result.cache

# Agent/editor state that is per-machine, not per-project. .claude/skills/ is
# committed, so this has to name the file rather than the directory.
.claude/settings.local.json

# Editor and tooling backups. .wp-env.json.bak-9930 was committed by accident
# and sat in the tree carrying a stale port until it was noticed.
*.bak
*.bak-*
*.orig
*.rej

# Release artifacts — built by bin/release/package.sh, attached to the GitHub
# release, never committed.
/laao-*.zip
/laao-*.zip.sha256

# Playwright
/tests/e2e/.results/
/playwright-report/
/test-results/

# Local environment
/.wp-env-backups/
/coverage/
/phpcs-report.txt

# Local tool caches (Lighthouse's Chrome profile lives here).
.cache/

# Lighthouse CI reports — generated by `pnpm perf`.
.lighthouseci/

# Chrome writes these under WSL when --user-data-dir is unset; see lighthouserc.cjs.
C:\\*
