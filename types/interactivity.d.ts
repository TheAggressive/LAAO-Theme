/**
 * Ambient types for the WordPress Interactivity API.
 *
 * Declared here rather than with a triple-slash reference inside a module:
 * the reference form is flagged by @typescript-eslint/triple-slash-reference,
 * and a project-level declaration applies to every Interactivity view script
 * instead of only the file that remembered to include it.
 */

/// <reference types="@wordpress/interactivity" />

export {};
