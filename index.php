<?php
/**
 * Block theme fallback.
 *
 * Block themes render through templates/*.html, so this file is only reached
 * if block template resolution fails entirely. WordPress still expects it to
 * exist, and Theme Check flags its absence.
 *
 * @package LAAO
 */

// Silence is golden.
